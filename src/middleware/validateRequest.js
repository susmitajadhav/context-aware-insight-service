// import { AppError } from '../errors/AppError.js';

// export const validateInsightRequest = (req, res, next) => {
//   const { tenantId, queryText } = req.body;

//   if (!tenantId || !queryText) {
//     throw new AppError('Invalid request payload', 400);
//   }

//   next();
// };